/*
 * current_sensor.c
 *
 *  Created on: Feb 18, 2016
 *      Author: Kin Man
 */
#include "current_sensor.h"

int read_current(unsigned char motor)
{
	int current;
	if (analogChannel > )
		current = analogRead(analogChannel);
	return current;
}

